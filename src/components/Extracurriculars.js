import React from "react"
import PropTypes from "prop-types"

export default class Extracurriculars extends React.Component {
  render() {
    return <p>extracurriculars</p>
  }
}

Extracurriculars.propTypes = {
  data: PropTypes.array
}
